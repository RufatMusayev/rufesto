{
  "name": "rufesto",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\" \"npm run dev:resto\"",
    "dev:client": "npm --prefix client run dev",
    "dev:resto": "npm --prefix client-resto run dev",
    "dev:server": "npm --prefix server run dev",
    "install:all": "npm install && npm --prefix client install && npm --prefix client-resto install && npm --prefix server install",
    "build": "npm --prefix client run build && npm --prefix client-resto run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
